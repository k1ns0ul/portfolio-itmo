apiVersion: batch/v1
kind: CronJob
metadata:
  name: {{ .Values.coupon.name }}
  namespace: {{ .Values.namespace }}
spec:
  schedule: "{{ .Values.coupon.schedule }}"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 2
      template:
        spec:
          restartPolicy: Never
          containers:
            - name: coupon
              image: "{{ .Values.image.registry }}/cfa-bonds-coupon-worker:{{ .Values.image.tag }}"
              imagePullPolicy: {{ .Values.image.pullPolicy }}
              args: ["--once"]
              envFrom:
                - configMapRef:
                    name: cfa-config
                - secretRef:
                    name: cfa-secrets
